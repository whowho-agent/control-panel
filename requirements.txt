# Runtime dependencies are managed in pyproject.toml / uv.lock.
# Keep this file for compatibility with existing Docker/CI steps until they are migrated.
fastapi==0.116.1
uvicorn[standard]==0.35.0
jinja2==3.1.6
python-multipart==0.0.20
pydantic==2.11.7
httpx==0.28.1
pytest==8.4.2
